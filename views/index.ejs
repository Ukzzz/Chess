<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <style>
      .chessboard {
        display: grid;
        grid-template-columns: repeat(8, 1fr);
        grid-template-rows: repeat(8, 1fr);
        width: 600px; /* fixed width */
        height: 600px; /* same height → perfect square */
        border: 6px solid #222;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.35);
        background: linear-gradient(145deg, #2c2c2c, #1a1a1a);
      }

      .square {
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2.4rem;
        font-weight: bold;
        user-select: none;
      }

      .light {
        background: #f5ecd9; /* beige */
      }

      .dark {
        background: #975a24; /* dark wood */
      }

      .square:hover {
        filter: brightness(1.08);
        transform: scale(1.03);
        z-index: 1;
      }

      .piece {
        cursor: grab;
        transition: transform 0.2s ease;
      }

      .piece:active {
        cursor: grabbing;
        transform: scale(1.15);
      }

      .chessboard.flipped {
        transform: rotate(180deg);
      }

      .chessboard.flipped .square {
        transform: rotate(180deg);
      }
      /* ✅ Optional: highlight squares for moves */
      .square.highlight {
        box-shadow: inset 0 0 0 3px #ffd700;
        background: radial-gradient(
          circle,
          rgba(255, 215, 0, 0.35) 30%,
          transparent 70%
        );
      }

      /* ✅ Optional: illegal move flash */
      .square.illegal {
        animation: flash 0.5s alternate 2;
      }

      @keyframes flash {
        from {
          background-color: #ff4d4d;
        }
        to {
          background-color: inherit;
        }
      }
    </style>
  </head>
  <body>
    <div class="w-full h-screen flex items-center justify-center bg-zinc-900">
      <div class="chessboard w-96 h-96"></div>
    </div>

    <script
      src="https://cdn.socket.io/4.8.1/socket.io.min.js"
      integrity="sha384-mkQ3/7FUtcGyoppY6bz/PORYoGqOl7/aSUMn2ymDOJcapfS6PHqxhRTMh1RR0Q6+"
      crossorigin="anonymous"
    ></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js"></script>
    <script src="../chessGame.js"></script>
  </body>
</html>
